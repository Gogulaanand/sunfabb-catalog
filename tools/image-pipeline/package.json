{
  "name": "image-pipeline",
  "private": true,
  "type": "module",
  "description": "Catalog image pipeline: scan source photos, split colorways, generate scene sets with Gemini, QA, review, upload to Cloudinary",
  "scripts": {
    "pipeline": "tsx src/cli.ts",
    "test": "node --import tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.0.0",
    "cloudinary": "^2.10.0",
    "dotenv": "^16.4.5",
    "sharp": "^0.34.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
